# Vite carga este archivo solo en modo desarrollo y tiene prioridad sobre .env,
# así que `npm run dev` habla siempre con la API local aunque .env apunte al
# despliegue. El build de producción sigue usando .env o la variable de Vercel.
VITE_API_URL=http://localhost:5000
