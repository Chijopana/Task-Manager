# Copy to .env and point it at your API. No trailing slash, no path.
VITE_API_URL=http://localhost:5000

# En desarrollo no hace falta tocar esto: .env.development ya apunta a
# http://localhost:5000 y tiene prioridad al ejecutar `npm run dev`.
